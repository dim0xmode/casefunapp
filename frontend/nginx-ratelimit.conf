# Rate limiting zones (defined in http context via conf.d inclusion).
# Key: real client IP forwarded by Caddy via X-Real-IP header.
limit_req_zone $http_x_real_ip zone=frontend_req:10m rate=30r/s;
limit_conn_zone $http_x_real_ip zone=frontend_conn:5m;
