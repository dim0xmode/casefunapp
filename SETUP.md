# Инструкция по запуску CaseFun

## Шаг 1: Установка необходимого ПО

Убедись, что установлено:
- **Node.js** (версия 18 или выше) - [скачать](https://nodejs.org/)
- **Docker Desktop** - [скачать](https://www.docker.com/products/docker-desktop/)

Проверь установку:
```bash
node --version    # должно быть v18+ 
npm --version
docker --version
```

## Шаг 2: Установка зависимостей

Открой терминал в папке проекта `/Users/satory/Downloads/casefun` и выполни:

```bash
# Установить зависимости для всех частей проекта
npm run install:all
```

Эта команда установит зависимости для:
- Корневого проекта
- Backend
- Frontend

## Шаг 3: Запуск базы данных

```bash
# Запустить PostgreSQL в Docker
npm run db:start
```

Подожди 10-15 секунд, пока база данных запустится.

Проверить что база запущена:
```bash
docker ps
```

Ты должен увидеть контейнер `casefun-db`.

## Шаг 4: Инициализация базы данных

```bash
# Перейти в папку backend
cd backend

# Создать таблицы в базе данных
npx prisma db push

# (Опционально) Открыть визуальный редактор БД
npx prisma studio
```

После `prisma db push` ты увидишь сообщение об успешном создании таблиц.

## Шаг 5: Запуск проекта

Вернись в корневую папку и запусти всё:

```bash
# Вернуться в корень проекта
cd ..

# Запустить backend + frontend одновременно
npm run dev
```

Откроются:
- **Backend API**: http://localhost:3001
- **Frontend**: http://localhost:5173

## ✅ Проверка работы

1. **Проверь backend:**
   Открой http://localhost:3001/api/health
   Должен вернуть: `{"status":"ok","timestamp":"..."}`

2. **Проверь frontend:**
   Открой http://localhost:5173
   Должен открыться сайт CaseFun

3. **Проверь подключение к MetaMask:**
   - Нажми "Connect Wallet" в шапке
   - Подключи кошелёк
   - Данные сохранятся в БД

## 🛑 Остановка проекта

```bash
# Остановить серверы (Ctrl+C в терминале где запущен npm run dev)

# Остановить базу данных
npm run db:stop
```

## 🔄 Сброс базы данных

Если нужно очистить все данные:

```bash
# ВНИМАНИЕ: Удалит ВСЕ данные!
npm run db:reset

# Потом заново инициализировать
cd backend && npx prisma db push
```

## 📊 Просмотр данных в БД

```bash
cd backend
npx prisma studio
```

Откроется визуальный интерфейс на http://localhost:5555

## ⚡ Быстрый старт (после первой установки)

После первого запуска, для последующих разов:

```bash
# 1. Запустить БД (если не запущена)
npm run db:start

# 2. Запустить проект
npm run dev
```

## 🐛 Решение проблем

### База данных не запускается
```bash
# Убедись что Docker Desktop запущен
# Проверь что порт 5432 свободен
npm run db:reset
```

### Ошибка подключения к БД в backend
```bash
# Проверь что .env файл существует
ls backend/.env

# Если нет, создай из примера
cp backend/.env.example backend/.env
```

### Frontend не видит backend
```bash
# Проверь что .env.local существует
ls frontend/.env.local

# Если нет, создай
echo "VITE_API_URL=http://localhost:3001/api" > frontend/.env.local
```

### Порты заняты
Если порты 3001 или 5173 заняты:
- Измени PORT в `backend/.env`
- Измени порт в `frontend/vite.config.js`

## 📝 Полезные команды

```bash
# Просмотр логов БД
docker logs casefun-db

# Подключиться к БД напрямую
docker exec -it casefun-db psql -U postgres -d casefun

# Только backend
npm run dev:backend

# Только frontend
npm run dev:frontend

# Билд для production
npm run build
```

## 🎯 Что дальше?

После запуска ты можешь:
1. Подключить MetaMask кошелёк
2. Создавать кейсы через админ-панель (будет добавлена)
3. Открывать кейсы
4. Смотреть данные в Prisma Studio

Все действия будут сохраняться в PostgreSQL базе данных!
