# 🔧 Решение проблем CaseFun

## Проблема: Сайт не открывается или белый экран

### Решение 1: Перезапусти серверы

```bash
# В терминале где запущен npm run dev, нажми Ctrl+C
# Затем запусти снова:
npm run dev
```

### Решение 2: Очисти кэш браузера

1. Открой http://localhost:5173
2. Нажми `Cmd + Shift + R` (принудительная перезагрузка)
3. Или открой в режиме инкогнито

### Решение 3: Проверь консоль браузера

1. Открой http://localhost:5173
2. Нажми `Cmd + Option + I` (открыть DevTools)
3. Перейди на вкладку **Console**
4. Ищи красные ошибки

Частые ошибки:
- `Failed to fetch` → Backend не отвечает
- `Cannot find module` → Не установлены зависимости
- `CORS error` → Проблема с настройками сервера

---

## Проблема: Backend не отвечает

### Проверка
```bash
curl http://localhost:3001/api/health
```

Должно вернуть: `{"status":"ok","timestamp":"..."}`

### Если не отвечает:

```bash
# Перезапусти backend
cd backend
npm run dev
```

---

## Проблема: База данных не работает

### Проверка
```bash
docker ps
```

Должен быть контейнер `casefun-db` со статусом `Up` и `(healthy)`

### Если не работает:

```bash
# Перезапусти БД
npm run db:stop
npm run db:start

# Подожди 15 секунд, затем проверь
docker ps
```

---

## Проблема: "Cannot connect to Docker daemon"

**Решение:** Запусти Docker Desktop

1. Открой приложение **Docker Desktop**
2. Подожди пока оно полностью запустится (иконка станет зелёной)
3. Попробуй снова

---

## Проблема: Порты заняты

### Ошибка: "Port 3001 already in use"

```bash
# Найди процесс занимающий порт
lsof -i :3001

# Останови его (замени PID на номер из предыдущей команды)
kill -9 <PID>
```

### Ошибка: "Port 5173 already in use"

```bash
# Найди процесс
lsof -i :5173

# Останови его
kill -9 <PID>
```

---

## Полный сброс (если ничего не помогло)

```bash
# 1. Останови всё
Ctrl+C  # в терминале где запущен проект
npm run db:stop

# 2. Очисти node_modules
rm -rf node_modules backend/node_modules frontend/node_modules

# 3. Переустанови зависимости
npm run install:all

# 4. Сброс базы данных (УДАЛИТ ВСЕ ДАННЫЕ!)
npm run db:reset
cd backend && npx prisma db push && cd ..

# 5. Запусти заново
npm run dev
```

---

## Диагностика

Открой тестовую страницу:
```bash
open test-connection.html
```

Она покажет статус всех компонентов.

---

## Всё ещё не работает?

1. Убедись что **Docker Desktop запущен**
2. Убедись что **порты 3001, 5173, 5432 свободны**
3. Проверь файл `backend/.env` - там должен быть DATABASE_URL
4. Проверь файл `frontend/.env.local` - там должен быть VITE_API_URL

### Показать все настройки:

```bash
# Backend
cat backend/.env

# Frontend  
cat frontend/.env.local

# Docker статус
docker ps -a
```

Если нужна помощь - пришли вывод этих команд!
